<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Assignment6_ListView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="ThemeNight" parent="Base.Theme.Assignment6_ListView">
        <item name="android:statusBarColor">@color/MainColor</item>
        <item name="android:background">#F4F4F4</item>
        <item name="android:textColor">#333333</item>
    </style>

    <style name="ThemeNight.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:statusBarColor">@color/MainColor</item>
        <item name="android:background">@color/MainColor</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>
</resources>